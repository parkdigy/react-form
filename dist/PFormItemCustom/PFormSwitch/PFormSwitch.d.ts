import React from 'react';
import { PFormSwitchProps as Props } from './PFormSwitch.types';
declare const PFormSwitch: ({ ref, variant: initVariant, size: initSize, color: initColor, focused: initFocused, hidden: initHidden, name, labelIcon, label, readOnly, disabled: initDisabled, error: initError, helperText, value: initValue, data: initData, exceptValue, onChange, onValidate, onValue, switchLabel, className, style, sx, }: Props) => React.JSX.Element;
export default PFormSwitch;
